import React from "react";

import "./ErrorRateGraph.css";

type User = {
  name: string;
};

export const ErrorRateGraph: React.FC = () => {
  const [user, setUser] = React.useState<User>();

  return <article></article>;
};
